<div class="container">
    <div class="row">
        <div class="co-md-3">
            <%= render 'users/info', user: current_user %>
            <%= render 'books/form', book: @book %>
        </div>
        <div class="col-md-8 off-set-1 mx-auto">
            <h2>Groups</h2>
         <%= render 'index', groups: @groups %>
        </div>
    </div>
</div>

